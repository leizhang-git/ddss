# DDSS 管理系统 - 开发设计文档

> 基于 RuoYi v3.9.0 改造，Spring Boot 2.5.15 + Vue 2 + Element UI

---

## 一、项目模块

| 模块 | 说明 |
|------|------|
| `ddss-bootstrap` | Spring Boot 入口 + REST Controller + Swagger + 中间件开关 |
| `ddss-common` | 工具类、注解、常量、过滤器、设计模式基类 |
| `ddss-ry-framework` | Security/JWT/AOP、Event 系统、Strategy 实现、线程池 |
| `ddss-system` | 系统业务：用户/角色/菜单/部门/字典/配置等 |
| `ddss-server` | 业务模块：财务管理、视频资源、Kafka、MinIO |
| `ddss-quartz` | 定时任务管理 |
| `ddss-generator` | 代码生成器 |
| `ddss-ui` | 前端 Vue2 + Element UI + ECharts |

---

## 二、中间件开关体系

统一在 `bootstrap.yml` 中（本地文件，不进 Nacos）：

```yaml
ddss:
  middleware:
    nacos:
      enabled: true   # false → 走本地配置
    redis:
      enabled: true   # false → RedisTemplate 不加载，RedisCache 降级
    kafka:
      enabled: true   # false → Kafka 不加载
    minio:
      enabled: true   # false → MinIO 不加载
    mysql:
      enabled: true   # false → 自动切 H2 内存库（执行 h2/schema.sql + h2/data.sql）
    xxl-job:
      enabled: true   # false → XXL-Job 不加载
  # 开发模式认证后门（仅本地无 MySQL 开发时置 true，生产必须 false）
  dev:
    login-enabled: false
```

关键实现类：
- `MiddlewareAutoConfigExcluder.java` — `EnvironmentPostProcessor`，根据开关动态排除 Spring Boot 自动配置、MySQL 关闭时自动换 H2 JDBC URL
- `H2DataInitializer.java` — `InitializingBean`，MySQL 关闭时执行 h2/schema.sql + h2/data.sql
- 各配置类：`RedisConfig`、`XxlJobConfig`、`MinioConfig`、`KafkaProducer/Customer/Pool` 均标注 `@ConditionalOnProperty`

---

## 三、设计模式

### Spring Events（替代 AsyncManager）
| 文件 | 说明 |
|------|------|
| `framework/event/LoginEvent.java` | 登录事件（username, status, message, request） |
| `framework/event/OperLogEvent.java` | 操作日志事件（SysOperLog） |
| `framework/event/EventPublisher.java` | 事件发布器 |
| `framework/listener/LoginEventListener.java` | `@Async @EventListener` 记录登录日志 |
| `framework/listener/OperLogEventListener.java` | `@Async @EventListener` 记录操作日志 |

### Strategy 模式
| 文件 | 说明 |
|------|------|
| `common/strategy/CaptchaStrategy.java` | 验证码策略接口 |
| `framework/strategy/MathCaptchaStrategy.java` | 数学验证码实现 |
| `framework/strategy/CharCaptchaStrategy.java` | 字符验证码实现 |
| `framework/strategy/CaptchaStrategyContext.java` | 策略选择器（按 captchaType） |
| `common/strategy/StorageStrategy.java` | 存储策略接口 |
| `framework/strategy/LocalStorageStrategy.java` | 本地文件存储 |
| `server/strategy/MinioStorageStrategy.java` | MinIO 存储 |

### Chain of Responsibility
| 文件 | 说明 |
|------|------|
| `common/chain/ValidationHandler.java` | 校验处理器接口 |
| `common/chain/AbstractValidationHandler.java` | 抽象链节点 |
| `common/chain/ValidationChain.java` | 链执行器 |
| `common/chain/validator/*.java` | NotEmpty/Length/XSS 具体校验器 |

### Composite 模式
| 文件 | 说明 |
|------|------|
| `common/composite/TreeNode.java` | 树节点接口 |
| `common/composite/TreeBuilder.java` | `buildTree()`, `findChildren()`, `getDescendants()`, `getAncestors()` |
| `common/composite/adapter/MenuNodeAdapter.java` | SysMenu → TreeNode 适配器 |
| `common/composite/adapter/DeptNodeAdapter.java` | SysDept → TreeNode 适配器 |

### Template Method
| 文件 | 说明 |
|------|------|
| `common/core/service/AbstractCrudService.java` | 泛型抽象 CRUD 基类，带缓存模板 |

### Builder
| 文件 | 说明 |
|------|------|
| `common/core/domain/AjaxResultBuilder.java` | 流式 AjaxResult 构建器 |

---

## 四、Redis 缓存体系

**核心类：`common/core/redis/RedisCache.java`**

| 防护 | 机制 | 方法 |
|------|------|------|
| 穿透 | 查不到数据缓存空值标记 `__NULL__`（5min TTL） | `setNullCache(key)`, `getOrSet()` |
| 击穿 | `SETNX` 互斥锁，单线程重建 | `tryLock()/unlock()`, `getOrSetWithLock()` |
| 雪崩 | TTL ±20% 随机偏移 | `setCacheObjectWithJitter()` |
| keys 阻塞 | `SCAN` 替代 `KEYS *` | `scanKeys()`, `deleteByPattern()` |
| Cache-Aside | 读缓存→未命中查库→回填 | `getOrSet(key, supplier, ttl)` |

**缓存消费方：**
- `SysMenuServiceImpl` — 菜单权限（`menu_perms:userId:*`, `menu_perms:roleId:*`），30min TTL，CRUD 时清除
- `SysRoleServiceImpl` — 角色权限（`role_perms:userId:*`），30min TTL
- `SysConfigServiceImpl` — 系统配置（`sys_config:*`），1h TTL，带抖动机动
- `DictUtils` / `SysDictTypeServiceImpl` — 字典缓存（`sys_dict:*`），2h TTL

---

## 五、财务管理模块

### 数据表 `sys_finance`

| 字段 | 类型 | 说明 |
|------|------|------|
| `finance_id` | bigint PK | 自增ID |
| `creditor_name` | varchar(100) | 欠款方名称 |
| `loan_amount` | decimal(12,2) | 借款总额 |
| `loan_date` | date | 借款日期 |
| `repayment_start_date` | date | 还款开始日期 |
| `repayment_end_date` | date | 还款结束日期 |
| `loan_term` | int | 借款期限(月) |
| `repayment_day` | int | 每月还款日 |
| `monthly_payment` | decimal(12,2) | 月还款额 |
| `interest_rate` | decimal(5,2) | 利率(%) |
| `interest_amount` | decimal(12,2) | 利息总额 |
| `early_settlement_amount` | decimal(12,2) | 提前结清金额 |
| `remaining_amount` | decimal(12,2) | 剩余未还金额 |
| `paid_amount` | decimal(12,2) | 已还金额 |
| `status` | char(1) | 0还款中 1已结清 2逾期 |
| `remark` | varchar(500) | 备注 |
| `paid_months` | varchar(500) | 已还月份（逗号分隔） |
| `month_data` | text | 月度明细 JSON（"25-7月": {amt:5000}） |
| `create_by/time` / `update_by/time` | | 审计字段 |

### 后端接口 `/finance/**`

| 接口 | 方法 | 权限 | 说明 |
|------|------|------|------|
| `/finance/list` | GET | finance:list | 分页列表（creditorName模糊, status精确） |
| `/finance/export` | GET | finance:list | Excel 导出（含动态月份列与合计行） |
| `/finance/{id}` | GET | finance:query | 单条详情 |
| `/finance` | POST | finance:add | 新增 |
| `/finance` | PUT | finance:edit | 修改 |
| `/finance/{ids}` | DELETE | finance:remove | 批量删除 |
| `/finance/summary` | GET | finance:stat | 汇总统计 |
| `/finance/groupByCreditor` | GET | finance:stat | 按欠款方分组 |
| `/finance/monthlyTrend` | GET | finance:stat | 月还款趋势 |

### 前端功能

**主列表（`views/finance/index.vue`）：**
- 列：名称、便宜、提前结清、总额、日期、几号、月份列(动态)、剩余、状态
- 列显隐控制、"列"按钮、排序、合计行
- 双击月份格弹出编辑框改金额，保存到 `month_data`
- 编辑后清缓存重算总额、已还、剩余

**统计页（`views/finance/statistics.vue`）：**
- 汇总卡片（笔数、总额、剩余、利息）
- ECharts 饼图（状态分布）柱图（欠款方）折线图（月趋势）
- 明细进度表（已还比例）

**自动计算规则：**
```
便宜 = max(0, 总额 - 提前结清金额)  // 两者都有值才计算
总额 = sum(所有月份金额) 或 月还款额 × 期限
剩余未还 = max(0, 借款总额 - 已还金额)
利息 = loanAmount × (interestRate/100) × (loanTerm/12)
月还款 = (loanAmount + interestAmount) / loanTerm
```

---

## 六、核心数据流

### 登录认证流程
```
POST /login → SysLoginController.login()
  → SysLoginService.login()
     → validateCaptcha() [跳过当 Redis 不可用时]
     → loginPreCheck() [IP黑名单校验]
     → AuthenticationManager.authenticate()
        → UserDetailsServiceImpl.loadUserByUsername()
           → SysUserMapper.selectUserByUserName()
           → SysPasswordService.validate() [BCrypt校验+错误次数]
        → SysPermissionService.getMenuPermission() [缓存菜单权限]
     → TokenService.createToken() [JWT + Redis存储]
     → EventPublisher.publishLoginEvent() [异步记录登录日志]
  → 返回 token
```

### 获取路由
```
GET /getRouters → SysLoginController.getRouters()
  → menuService.selectMenuTreeByUserId()
  → menuService.buildMenus() [递归构建树]
```

### Dev 模式（MySQL/Redis/Nacos 全关）
```
启动时：
  bootstrap.yml → ddss.middleware.*.enabled = false，并手动置 ddss.dev.login-enabled = true
  MiddlewareAutoConfigExcluder → 排除各中间件自动配置，切换 H2 内存库
  H2DataInitializer → 执行 h2/schema.sql + h2/data.sql 初始化表与数据
  JwtAuthenticationTokenFilter / SysLoginController → 当 mysql=false 且 dev.login-enabled=true 时，
    对未携带 token 的请求自动注入 admin 身份（/login 免密返回 admin token）
  admin 拥有 ALL_PERMISSION，PermissionService.hasPermi() 对 admin 直接放行
```
> ⚠️ 安全提示：`ddss.dev.login-enabled` 默认 false，仅用于本地无中间件开发。生产环境必须保持 false，
> 否则任何请求将以 admin 身份放行。开启时启动日志会打印 WARN 告警。

---

## 七、配置文件

| 文件 | 路径 |
|------|------|
| 中间件开关 | `ddss-bootstrap/src/main/resources/bootstrap.yml` |
| 本地兜底配置（MySQL/Redis/Kafka等完整配置） | `ddss-bootstrap/src/main/resources/application-local.yml` |
| Nacos 配置 | `nacos://DDSS_GROUP/ddss-server.yaml` + `ddss-server-druid.yaml` |
| 代码生成 | `ddss-generator/src/main/resources/generator.yml` |
| H2 初始化 SQL | `ddss-bootstrap/src/main/resources/h2/schema.sql` + `data.sql` |
| 前端 .env 变量 | `ddss-ui/.env.development / .env.production / .env.staging` |
| 数据库全量脚本 | `db/ddss.sql` |
| 部署脚本 | `deploy.ps1` |

---

## 八、前端路由规则

- **常量路由**：`/login`, `/register`, `/index`, `/404`, `/401`, `/user/profile`（硬编码）
- **动态路由**：通过 `GET /getRouters` 从后端加载，以 `sys_menu` 表中的菜单树构建
- 前端组件路径 = `views/{componentPath}.vue`（如 `finance/index` → `views/finance/index.vue`）

---

## 九、数据库表清单

| 表 | 描述 |
|-----|------|
| `sys_user` | 用户 |
| `sys_role` | 角色 |
| `sys_role_menu` | 角色菜单 |
| `sys_role_dept` | 角色部门 |
| `sys_menu` | 菜单 |
| `sys_dept` | 部门 |
| `sys_dict_type` | 字典类型 |
| `sys_dict_data` | 字典数据 |
| `sys_config` | 系统参数 |
| `sys_notice` | 通知公告 |
| `sys_post` | 岗位 |
| `sys_user_post` | 用户岗位 |
| `sys_user_role` | 用户角色 |
| `sys_oper_log` | 操作日志 |
| `sys_logininfor` | 登录日志 |
| `sys_job` | 定时任务 |
| `sys_job_log` | 任务日志 |
| `sys_finance` | 财务管理 |
| `ddss_video` | 视频管理 |
| `ddss_resource` | 资源管理 |
| `sys_video_resource` | 视频资源关联 |
| `gen_table` | 代码生成表 |
| `gen_table_column` | 代码生成列 |
| `qrtz_*` | Quartz 调度表 |
