# 视频管理功能 - 问题排查和解决指南

## 🎯 当前状态
- ✅ 后端代码已完成（Controller、Service、Mapper、Entity）
- ✅ 前端页面已存在（index.vue）
- ✅ API接口正常（返回401说明接口存在但需要登录）
- ❌ **可能的问题**：数据库表未创建 或 菜单未配置

---

## 🔧 解决方案（按步骤执行）

### 步骤1️⃣：连接MySQL数据库

使用Navicat、DBeaver、MySQL Workbench或命令行连接到你的MySQL服务器：
- **主机**: 192.168.52.100
- **端口**: 3306
- **用户名**: root
- **密码**: 123456

### 步骤2️⃣：执行完整初始化脚本

**方法A：使用SQL客户端工具（推荐）**

1. 打开 `D:\code\ddss\db\video_complete_setup.sql` 文件
2. 在SQL客户端中执行该文件

**方法B：分步手动执行**

#### 2.1 在 ry-vue 数据库中执行菜单配置

```sql
-- 切换到系统数据库
USE ry-vue;

-- 删除可能存在的旧数据
DELETE FROM sys_menu WHERE menu_id IN ('5', '2000', '2001', '2002', '2003', '2004');

-- 插入一级菜单：视频管理
INSERT INTO sys_menu VALUES ('5', '视频管理', '0', '5', 'video', null, '', '', 1, 0, 'M', '0', '0', '', 'video', 'admin', sysdate(), '', null, '视频管理目录');

-- 插入二级菜单：视频列表
INSERT INTO sys_menu VALUES ('2000', '视频列表', '5', '1', 'list', 'video/index', '', '', 1, 0, 'C', '0', '0', 'video:list', 'video-camera', 'admin', sysdate(), '', null, '视频列表菜单');

-- 插入按钮权限
INSERT INTO sys_menu VALUES ('2001', '视频查询', '2000', '1', '#', '', '', '', 1, 0, 'F', '0', '0', 'video:query', '#', 'admin', sysdate(), '', null, '');
INSERT INTO sys_menu VALUES ('2002', '视频新增', '2000', '2', '#', '', '', '', 1, 0, 'F', '0', '0', 'video:add', '#', 'admin', sysdate(), '', null, '');
INSERT INTO sys_menu VALUES ('2003', '视频修改', '2000', '3', '#', '', '', '', 1, 0, 'F', '0', '0', 'video:edit', '#', 'admin', sysdate(), '', null, '');
INSERT INTO sys_menu VALUES ('2004', '视频删除', '2000', '4', '#', '', '', '', 1, 0, 'F', '0', '0', 'video:remove', '#', 'admin', sysdate(), '', null, '');

-- 验证
SELECT * FROM sys_menu WHERE menu_id IN ('5', '2000', '2001', '2002', '2003', '2004');
```

#### 2.2 在 ddss 数据库中创建视频表

```sql
-- 切换到业务数据库
USE ddss;

-- 创建视频表
DROP TABLE IF EXISTS `ddss_video`;
CREATE TABLE `ddss_video` (
  `video_id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT '视频ID',
  `video_name` varchar(100) NOT NULL COMMENT '视频名称',
  `video_url` varchar(500) NOT NULL COMMENT '视频URL',
  `cover_image` varchar(500) DEFAULT NULL COMMENT '封面图URL',
  `duration` int(11) DEFAULT NULL COMMENT '时长(秒)',
  `file_size` varchar(50) DEFAULT NULL COMMENT '文件大小',
  `status` char(1) DEFAULT '0' COMMENT '状态（0正常 1停用）',
  `remark` varchar(500) DEFAULT NULL COMMENT '备注',
  `create_by` varchar(64) DEFAULT '' COMMENT '创建者',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_by` varchar(64) DEFAULT '' COMMENT '更新者',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`video_id`),
  KEY `idx_video_name` (`video_name`)
) ENGINE=InnoDB AUTO_INCREMENT=1 DEFAULT CHARSET=utf8mb4 COMMENT='视频管理表';

-- 验证表结构
DESCRIBE ddss_video;

-- 插入测试数据（可选）
INSERT INTO ddss_video (video_name, video_url, cover_image, duration, file_size, status, remark, create_by) 
VALUES 
('测试视频1', 'https://www.w3schools.com/html/mov_bbb.mp4', 'https://via.placeholder.com/400x200?text=Video1', 120, '15MB', '0', '这是一个测试视频', 'admin'),
('测试视频2', 'https://www.w3schools.com/html/movie.mp4', 'https://via.placeholder.com/400x200?text=Video2', 90, '12MB', '0', '这是另一个测试视频', 'admin');

-- 验证数据
SELECT * FROM ddss_video;
```

### 步骤3️⃣：重启后端服务

由于添加了新的Controller，建议重启Spring Boot应用以确保所有组件被正确加载。

**如果后端已经在运行：**
1. 停止当前运行的Java进程（进程ID: 84292）
2. 重新启动应用

**重启命令示例：**
```bash
# 找到并停止进程
taskkill /F /PID 84292

# 然后重新启动你的Spring Boot应用
```

### 步骤4️⃣：重新登录前端系统

1. 打开浏览器访问：`http://localhost`
2. 如果已经登录，先退出登录
3. 重新登录（账号：admin，密码：admin123 或你设置的密码）
4. **清除浏览器缓存**（Ctrl+Shift+Delete 或 F12开发者工具 → Network → Disable cache）
5. 刷新页面（F5）

### 步骤5️⃣：查看视频管理菜单

登录后，应该在左侧菜单看到：
```
📹 视频管理
   └─ 📋 视频列表
```

点击"视频列表"即可进入视频管理页面。

---

## ✅ 验证清单

执行完以上步骤后，请确认：

- [ ] MySQL中 `ry-vue.sys_menu` 表有6条新记录（menu_id: 5, 2000-2004）
- [ ] MySQL中 `ddss.ddss_video` 表已创建
- [ ] 后端服务已重启
- [ ] 浏览器已清除缓存并重新登录
- [ ] 左侧菜单显示"视频管理"
- [ ] 点击"视频列表"能看到表格（即使为空或有测试数据）
- [ ] 搜索框可以输入关键词进行模糊查询
- [ ] 分页组件正常显示
- [ ] 可以点击"新增"按钮添加视频

---

## 🐛 常见问题排查

### 问题1：看不到"视频管理"菜单

**原因**：菜单SQL未执行或未生效

**解决**：
1. 检查 `ry-vue.sys_menu` 表是否有对应记录
2. 退出登录，清除浏览器缓存
3. 重新登录

### 问题2：点击菜单后页面空白或报错

**原因**：后端服务未重启或API调用失败

**解决**：
1. 检查后端日志是否有错误
2. 打开浏览器开发者工具（F12）→ Console 和 Network 标签
3. 查看是否有API请求失败
4. 确认后端服务正在运行

### 问题3：API返回403 Forbidden

**原因**：当前用户没有视频管理权限

**解决**：
1. 确认使用的是管理员账号登录
2. 检查角色权限配置
3. 给用户角色分配视频管理相关权限

### 问题4：数据库连接失败

**原因**：数据库配置错误或网络不通

**解决**：
1. 确认MySQL服务正在运行
2. 检查数据库连接配置（db.yaml）
3. 测试能否ping通 192.168.52.100
4. 确认防火墙允许3306端口

---

## 📞 获取帮助

如果按照以上步骤仍然无法解决，请提供以下信息：

1. **浏览器控制台错误截图**（F12 → Console）
2. **Network请求详情**（F12 → Network → 点击失败的请求 → Headers和Response）
3. **后端日志**（查看启动日志和访问日志）
4. **数据库执行结果**（SQL执行是否成功）

---

## 🎉 成功后效果

完成所有步骤后，你将拥有：

✅ 完整的视频管理功能  
✅ 支持分页查询  
✅ 支持按视频名称模糊检索  
✅ 支持按状态筛选  
✅ 支持新增、修改、删除操作  
✅ 完善的权限控制  
✅ 操作日志记录  

祝你使用愉快！🚀
